import React from 'react';
import {Row} from 'react-bootstrap';
import styles from './registerHeader.css';

const RegisterHeader = () => (
  <Row>
    <h1 className={styles.h1}>Register</h1>
  </Row>
);

export default RegisterHeader;
